# Production build — loaded automatically by `npm run build` (Vite mode=production).
# Absolute base on the SAME host that serves the built SPA => same-origin, no CORS.
VITE_API_URL=https://client.bayareaimmigrationservices.com/api
VITE_INSZOOM_URL=https://admin.bayareaimmigrationservices.com
