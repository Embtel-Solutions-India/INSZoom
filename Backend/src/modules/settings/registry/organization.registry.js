const { z } = require("zod");

// §4.1 Organization & Localization. Consumed by a shared formatting util
// (settings/formatting.util.js) and by SLA due-date math (workflow.registry
// business hours).
module.exports = [
  {
    key: "org.name",
    category: "organization",
    group: "Identity",
    label: "Organization name",
    description: "The firm/organization's display name across the staff CRM.",
    type: "string",
    default: "Immiglance",
    validation: z.string().min(1).max(200),
    scopes: ["system"],
    requiresPermission: "settings:manage_general",
    affects: "formatting.util / document headers",
  },
  {
    key: "org.timezone",
    category: "organization",
    group: "Localization",
    label: "Default timezone",
    description: "IANA timezone used for SLA due-date math and displayed timestamps when a user has no personal override.",
    type: "string",
    default: "America/New_York",
    validation: z.string().min(1),
    scopes: ["system", "user"],
    requiresPermission: "settings:manage_general",
    affects: "formatting.util, workflow SLA engine",
  },
  {
    key: "org.locale",
    category: "organization",
    group: "Localization",
    label: "Locale",
    description: "BCP 47 locale used for number/date formatting.",
    type: "string",
    default: "en-US",
    validation: z.string().min(2).max(20),
    scopes: ["system", "user"],
    requiresPermission: "settings:manage_general",
    affects: "formatting.util",
  },
  {
    key: "org.dateFormat",
    category: "organization",
    group: "Localization",
    label: "Date format",
    description: "Display format for dates across the CRM.",
    type: "enum",
    enumValues: ["MM/DD/YYYY", "DD/MM/YYYY", "YYYY-MM-DD"],
    default: "MM/DD/YYYY",
    validation: z.enum(["MM/DD/YYYY", "DD/MM/YYYY", "YYYY-MM-DD"]),
    scopes: ["system", "user"],
    requiresPermission: "settings:manage_general",
    affects: "formatting.util",
  },
  {
    key: "org.numberFormat",
    category: "organization",
    group: "Localization",
    label: "Number format",
    description: "Thousands/decimal separator style.",
    type: "enum",
    enumValues: ["1,234.56", "1.234,56", "1 234.56"],
    default: "1,234.56",
    validation: z.enum(["1,234.56", "1.234,56", "1 234.56"]),
    scopes: ["system", "user"],
    requiresPermission: "settings:manage_general",
    affects: "formatting.util",
  },
  {
    key: "org.fiscalYearStart",
    category: "organization",
    group: "Localization",
    label: "Fiscal year start month",
    description: "Month (1-12) the fiscal year begins, used by revenue/reporting periods.",
    type: "number",
    default: 1,
    validation: z.number().int().min(1).max(12),
    scopes: ["system"],
    requiresPermission: "settings:manage_general",
    affects: "reports module (period boundaries)",
  },
  {
    key: "org.businessHours",
    category: "organization",
    group: "Localization",
    label: "Business hours",
    description: "Working hours used by SLA due-date math (weekends/after-hours are excluded from SLA clocks).",
    type: "json",
    default: { start: "09:00", end: "18:00", days: [1, 2, 3, 4, 5] },
    validation: z.object({
      start: z.string().regex(/^\d{2}:\d{2}$/),
      end: z.string().regex(/^\d{2}:\d{2}$/),
      days: z.array(z.number().int().min(0).max(6)),
    }),
    scopes: ["system"],
    requiresPermission: "settings:manage_general",
    affects: "workflow SLA engine",
  },
];
