# Production build — loaded automatically by `npm run build` (Vite mode=production).
# Absolute base on the SAME host that serves the built admin SPA => same-origin, no CORS.
VITE_API_URL=https://admin.bayareaimmigrationservices.com/api
